# 필수 설치 목록
## Whisper(STT 엔진)
```python
  py -m pip install git+https://github.com/openai/whisper.git

```

## PyTorch (Whisper가 내부적으로 사용)
```python
py -m pip install torch

```

## SoundDevice
```python
py -m pip install sounddevice

```

## Scipy (녹음 파일 저장용)
```python
py -m pip install scipy

```

## py -m pip install keyboard
```python
py -m pip install keyboard

```
